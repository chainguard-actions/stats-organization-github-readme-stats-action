name: Test stats card generation
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          card: stats
          options: "username=anuraghazra"
          path: "output/stats.svg"
      - name: Assert output path matches
        env:
          ACTUAL: ${{ steps.action.outputs.path }}
        run: |
          echo "actual path: $ACTUAL"
          if [ "$ACTUAL" != "output/stats.svg" ]; then
            echo "FAIL: expected 'output/stats.svg' but got '$ACTUAL'"
            exit 1
          fi
          echo "PASS: output path matches"
      - name: Assert SVG file exists and contains SVG content
        run: |
          if [ ! -f "output/stats.svg" ]; then
            echo "FAIL: SVG file not found at output/stats.svg"
            exit 1
          fi
          if ! grep -q "<svg" "output/stats.svg"; then
            echo "FAIL: file does not contain SVG content"
            cat "output/stats.svg"
            exit 1
          fi
          echo "PASS: SVG file exists and contains SVG content"
